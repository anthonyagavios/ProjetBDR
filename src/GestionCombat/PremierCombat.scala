import GestionCombat.{PartySolar, PartyWyrm}
/*
var gentil = new PartySolar(1, 0, 0, 0)
var mechant = new PartyWyrm(0, 4, 0, 9, 1)

// Attaque a distance tant que les orc ne sont pas au corp a corp
// Attaque toujours la meme cible tant que celle ci est vivante
// Attaque l'ennemie avec le plus grand nombre de HP
if (distance > 7) {

  gentil.solar(0).attaque("ranged", mechant, mechant.barbareOrc(0).name, 0)

}
// Attaque au corp a corp
else if (distance < 7) {

  gentil.solar(0).attaque("melee",mechant,mechant.barbareOrc(0).name, 0)
}

// Fuite



// Deplacement

*/
